---
trigger: always_on
---

Rule: On the main dashboard/homepage route, standard gamification metrics (total XP bars, currency counts) must be visually de-emphasized or hidden by default, accessible only via a secondary click (e.g., into a Profile section). The primary view must focus on the next immediate action (the "Now").

Reasoning: Enforces the new design direction you provided in the mockup requirements.